classdef VS_mode_Cl
    %% VS_MODE_Cl Variant Subsystem Mode Cl
    %   stores strings to set the modes of variant subsystems to configure
    %   the model for different tests (generic)
    
    %% mode parameters
    properties
        CH (1,1) string;
        jounce (1,1) string;
    end
    
end